<?xml version="1.0" encoding="utf-8"?>
<manifest xmlns:android="http://schemas.android.com/apk/res/android">

    <uses-permission android:name="android.permission.CAMERA" />
    <uses-permission android:name="android.permission.RECORD_AUDIO" />
    <uses-permission android:name="android.permission.READ_EXTERNAL_STORAGE" />
    <uses-permission android:name="android.permission.READ_MEDIA_IMAGES" />
    <uses-permission android:name="android.permission.READ_MEDIA_VIDEO" />
    <uses-permission android:name="android.permission.READ_MEDIA_AUDIO" />

    <!-- Permissions are optional: the attachment pickers degrade gracefully
         when the hardware is absent (returns no matching sources).
         required=false keeps installs working on devices without them. -->
    <uses-feature android:name="android.hardware.camera" android:required="false" />
    <uses-feature android:name="android.hardware.microphone" android:required="false" />

    <application
        android:name=".LettersApplication"
        android:allowBackup="true"
        android:icon="@mipmap/ic_launcher"
        android:label="Letters to My"
        android:theme="@style/Theme.LettersToMy"
        android:usesCleartextTraffic="true">
        <!-- usesCleartextTraffic mirrors iOS NSAllowsLocalNetworking: the
             self-hosted sync server is user-configured and routinely lives on
             a plain-HTTP LAN endpoint (e.g. http://10.0.2.2:8080 from the
             emulator, or a Docker box on the home network). API tokens are
             still stored in the Keystore; only the archive/API payloads travel
             this channel. -->

        <activity
            android:name=".MainActivity"
            android:exported="true"
            android:windowSoftInputMode="adjustResize">
            <intent-filter>
                <action android:name="android.intent.action.MAIN" />
                <category android:name="android.intent.category.LAUNCHER" />
            </intent-filter>
        </activity>
    </application>
</manifest>